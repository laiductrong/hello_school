export interface Teacher {
    teacherId: number;
    name: string;
    birthDate: string;
    address: string;
    phoneNumber: string;
    email: string;
    subjectId: number;
    subjectName: string;
    
}