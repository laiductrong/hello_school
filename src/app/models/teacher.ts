export interface Teacher {
    teacherId: number;
    name: string;
    birthDate: Date;
    address: string;
    phoneNumber: string;
    email: string;
    subjectId: number;
    subjectName: string;
}